<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />

    <title>{% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}</title>
    {% if meta.description %}
        <meta name="description" content="{{ meta.description|striptags }}">
    {% endif %}{% if meta.robots %}
        <meta name="robots" content="{{ meta.robots }}">
    {% endif %}
	
	<link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css">
</head>


<body>
<div class="layout">
	<div id="header">
		<a href="{{ "index"|link }}">{{ site_title }}</a>
	</div>
	<div class="wrapper">
	<div id="navbar">
		<ul class="nav">
			{% for page in pages %}
			   {% if page.meta.toc  == "section" %}	
				<li{% if page.id == current_page.id %} id="active"{% endif %}>
				  <a href="{{ page.url }}">{{ page.title }} {% if page.id == current_page.id %} &laquo; {% endif %}</a>
				</li>
			   {% endif  %}
			{% endfor %}
		
		<hr color="#ccc" width="20%" align="right">
			{% for page in pages %}
			   {% if page.meta.template  == "blog" %}	
				<li{% if page.id == current_page.id %} id="active"{% endif %}>
				  <a href="{{ page.url }}">{{ page.title }} {% if page.id == current_page.id %} &laquo; {% endif %}</a>
				</li>
			   {% endif  %}
			{% endfor %}

		<hr color="#ccc" width="20%" align="right">
                        {% for page in pages %}
                           {% if page.meta.toc  == "appendix" %}
                                <li{% if page.id == current_page.id %} id="active"{% endif %}>
                                  <a href="{{ page.url }}">{{ page.title }} {% if page.id == current_page.id %} &laquo; {% endif %}</a>
                                </li>
                           {% endif  %}
                        {% endfor %}

		</ul>

	</div>
	<div id="content">
		<div class="inner">
			{{ content }}
		</div>
	</div>
  </div>
</div> 
</body>
</html>
